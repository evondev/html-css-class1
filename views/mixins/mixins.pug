mixin menuItem(href, title, icon)
  li.menu-item
    a.menu-link(href= href)
      span.menu-icon
        img(src= icon, alt="")
      span= title

mixin hostItem(image, name, bg)
  li.host-item
    .host-avatar(style=`background-color: ${bg}`)
      img(src= image, alt)
    span.host-name= name